/**
 * Default URL for in-app browser panels when no URL is stored or chosen
 * (embedded agent sidebar, project sidebar “open browser”).
 */
export const DEFAULT_BROWSER_HOME_URL = "https://acepe.dev";
